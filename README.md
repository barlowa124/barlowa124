# Angus Barlow (barlowa124)

Biological data → predictive ML → scientific agents → independently checkable execution → regulated biopharma software.

I build life-science software where the evidence is inspectable: frozen splits, deterministic computation, machine-checked claims, replayable runs, and explicit abstention when the data does not support a result.

Currently focused on **trustworthy AI and human-relevant preclinical models**: New Approach Methodologies (NAMs), computational toxicology, and ML tooling for drug discovery that reduces reliance on animal testing.

## Where to start

- **Uncertainty and calibration**: [comp-tox-pipeline](https://github.com/barlowa124/comp-tox-pipeline) (scaffold-split conformal + applicability domain on molecules), [protein-stability-uncertainty](https://github.com/barlowa124/protein-stability-uncertainty) (homology-split conformal, Mondrian per-domain coverage on proteins).
- **Debugging model failures**: [llm-posttraining](https://github.com/barlowa124/llm-posttraining) (DPO collapse → zero-gradient diagnosis → GRPO repair, raw generations committed), [protein-diffusion](https://github.com/barlowa124/protein-diffusion) (memorization forensics → fitness-conditioned fix).
- **Experiment design**: [active-learning-loop](https://github.com/barlowa124/active-learning-loop) (GP-UCB vs random, two real measured landscapes, encoder ablation).
- **Operationalization**: [oncology-coscientist](https://github.com/barlowa124/oncology-coscientist) (deterministic verifier + human gate), [dockops](https://github.com/barlowa124/dockops), [bioprocess-decision-runtime](https://github.com/barlowa124/bioprocess-decision-runtime).

## Projects

| Repository | What it is | What to look at |
|---|---|---|
| [oncology-coscientist](https://github.com/barlowa124/oncology-coscientist) | Cohort-agnostic TCGA survival analysis (Cox PH, Random Survival Forest) with LangGraph agents that draft reports while a deterministic verifier binds every number in the prose to the computation and to the model it is attributed to. Human approval gate, replayable transcripts. | The "What the verifier caught" section: fabricated cohort sizes, invented metrics for abstained models, misattributed model blocks. |
| [bioprocess-decision-runtime](https://github.com/barlowa124/bioprocess-decision-runtime) | Independently executable, bit-exact specification of a fixed Gemma 3 270M checkpoint (all 18 layers, full 262,144-logit vocabulary projection) with predeclared held-outs, replay, and a preserved failure where token agreement hid 49 differing logits. Plus an advisory-only evidence-bound policy runtime for a synthetic bioprocess scenario. | Results-at-a-glance table; `docs/EXPERIMENT_LOG.md` for the full record. |
| [cultivated-meat-multiomic](https://github.com/barlowa124/cultivated-meat-multiomic) | Methods demonstration: RNA + metabolic-flux clustering, 30-gene panel selection, calibration, conformal prediction, ablation and drift monitoring on public data; cross-species checks on bovine and porcine muscle. | Data-provenance section (explicit about the stand-in dataset); integration benchmark. |
| [labStackDev](https://github.com/barlowa124/labStackDev) | Production-style RNA-seq quantification pipelines (Salmon, STAR+Salmon hybrid, pyDESeq2, METAFlux), validated at r = 0.984 against a CLC baseline. | Pipeline README with timings and validation. |
| [comp-tox-pipeline](https://github.com/barlowa124/comp-tox-pipeline) | Reproducible Tox21 evaluation: scaffold-split LR/RF/GIN with calibration, conformal coverage, and applicability-domain conditioning. Includes a JAX/Flax port verified for numerical parity with PyTorch and real multi-process DDP training. | Per-endpoint metrics; the documented NR-ER applicability-domain inversion that only showed up because out-of-domain metrics were measured. |
| [dockops](https://github.com/barlowa124/dockops) | Docking pipeline ops: DUD-E benchmark staging, ligand-derived boxes, RDKit/Meeko prep, Vina backend, AFDB-vs-PDB structure QC (offset-aware Cα RMSD), OpenMM mechanics check, FastAPI job service with per-run provenance manifests. | `results/structure_qc.json`, where the QC correctly argues *against* docking into the AF model for EGFR. |
| [organoid-qc](https://github.com/barlowa124/organoid-qc) | Organoid fidelity scoring: organoid clusters vs CELLxGENE reference centroids (coverage, unmapped fraction, marker detection, QC flags). Plus a simulated Opentrons Flex dosing protocol validated step-by-step by the official Protocol Engine. | The real-data intestine result (coverage 1.0, stem-marker shortfall reported) next to the synthetic demo that fails by design. |
| [protein-design-ops](https://github.com/barlowa124/protein-design-ops) | Backbone-conditioned design: ProteinMPNN generation + independent ESM-2 masked-marginal rescoring + consensus report, two backbones, pinned-seed provenance. | The reproducibility caveat, where upstream `--seed 0` silently randomizes. Caught by rerunning and diffing artifacts. |
| [active-learning-loop](https://github.com/barlowa124/active-learning-loop) | Experiment-selection simulation on two real measured landscapes (GB1 149k + AAV2 38k variants): GP-UCB acquisition vs a 20-seed random baseline under an identical budget, replicated over seeds; encoder ablation (one-hot vs ESM-2-in-context) included. | The debugging trail: the first run silently degenerated to index-order picks via NaN acquisition scores, caught by the frozen best-fitness curve. ~79x top-100 enrichment on GB1 after the fix. On the harsher AAV landscape the hit enrichment holds (~8x) but the AUBC bands overlap, a partial replication documented as such. ESM-2 embeddings don't beat one-hot on either landscape; on AAV they lose the AL edge entirely because mean-pooled embeddings smooth over the Hamming structure the GP exploits. |
| [protein-diffusion](https://github.com/barlowa124/protein-diffusion) | Conditional DDPM + classifier-free guidance over the GB1 fitness landscape, scored against the measured oracle. | The full arc: the v1 unconditional model memorized (46% copied rows, novel variants at random fitness), root-caused to a non-smooth landscape. v2 adds fitness conditioning and steers measurably (80% fit vs 4% random, dose-response committed). Benchmarked against the trivial baseline (mutating elite parents): diffusion wins bulk enrichment but loses on unique top-100 discovery, the CFG diversity cost. AAV2 transfer fails twice: the oracle is vacuous (100% unmeasured space) and the model underfits the manifold. Both failures are deconstructed with diagnostics. |
| [dti-fusion](https://github.com/barlowa124/dti-fusion) | Multimodal drug-target interaction on DAVIS: drug fingerprints + ESM-2 protein embeddings with modality ablations, evaluated on held-out proteins. | The ablation table: fusion wins on ranking/MSE and drug-only edges it on MAE. |
| [protein-stability-uncertainty](https://github.com/barlowa124/protein-stability-uncertainty) | Sequence → melting-point regression on the Meltome atlas (27,951 proteins): ESM-2 vs composition features, ridge, split-conformal and Mondrian conformal intervals, on a 20%-identity homology-separated split. Thermal stability is a food/protein-engineering trait. | The applicability-domain arc: marginal conformal coverage (0.90) hides a gradient (0.96 near → 0.85 far). Per-AD-bin Mondrian calibration recovers flat coverage by widening intervals with distance (9.8 → 14.5 °C). The composition-feature AD is flat, so the embedding distance is what carries the signal. |
| [llm-posttraining](https://github.com/barlowa124/llm-posttraining) | SFT + hand-rolled DPO + GRPO on SmolLM2-135M: abstention-vs-fabrication task, four-stage measured eval on held-out entities. | Full arc measured: DPO collapses deployed behavior at 100% train accuracy, binary rewards give RL zero gradient, shaped GRPO repairs the collapse. Raw generations are committed. Ablations: the repair replicates across rollout seeds, and GRPO from a healthy policy is provably inert (60/60 no-signal steps, bit-identical output). |
| [vector-db-mcp](https://github.com/barlowa124/vector-db-mcp) | Local vector database exposing the Pinecone API surface over MCP (stdio): indexes, namespaces, metadata filters, upsert/query/fetch/delete/update, optional local embedding for text queries. | The honest boundary is the README's "not implemented" list — exact linear scan instead of ANN, disk instead of a cluster. Feature parity with the hosted surface; the differences are scale and networking only. |
| [qms-ai-system-resume](https://github.com/barlowa124/qms-ai-system-resume) | Fail-closed deployment assessment engine and hardened reference architecture for AI in regulated quality systems. | Threat model and assessment engine tests. |

**Upstream contributions:** [TDC fork](https://github.com/barlowa124/TDC), a tested fix for silent dataset-name substitution plus an AnnData getter/split API. [ProteinMPNN fork](https://github.com/barlowa124/ProteinMPNN), an effective-seed visibility fix filed upstream as [issue #154](https://github.com/dauparas/ProteinMPNN/issues/154).

## Scope statements I hold to

- Verified computation is not verified science: reproducing a calculation says nothing about whether the model or the hypothesis is right.
- Research and education use only. Nothing here is validated for GMP, clinical, or manufacturing decisions.
- Failures are kept, not deleted. Rejected runs and abstentions are part of the evidence record.
